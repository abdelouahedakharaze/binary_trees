#include "binary_trees.h"

/**
 * heap_to_sorted_array - converte un Binary Max Heap in un array ordinato di interi
 *
 * @root: puntatore doppio al nodo radice dell'heap
 * Return: valore memorizzato nel nodo radice
 */
int heap_extract(heap_t **root)
{
    return (root);
}
