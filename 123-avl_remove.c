#include "binary_trees.h"

/**
 * avl_remove - rimuove un nodo da un albero AVL
 *
 * @root: puntatore al nodo radice dell'albero per rimuovere un nodo
 * @value: valore da rimuovere dall'albero
 * Return: puntatore alla nuova radice dell'albero
 * dopo aver rimosso il valore desiderato e dopo il riequilibrio
 */
avl_t *avl_remove(avl_t *root, int value)
{
    return (root);
}
