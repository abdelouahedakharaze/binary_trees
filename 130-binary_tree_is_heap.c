#include "binary_trees.h"

/**
 * binary_tree_is_heap - verifica se un albero binario
 * è un Max Binary Heap valido
 *
 * @tree: Puntatore al nodo dell'albero
 *
 * Return: 1 se l'albero è un max heap, 0 altrimenti
 */
int binary_tree_is_heap(const binary_tree_t *tree)
{
    return (tree);
}
