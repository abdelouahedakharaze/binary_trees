#include "binary_trees.h"

/**
 * heap_to_sorted_array - converte un Binary Max Heap in un array ordinato di interi
 *
 * @heap: puntatore al nodo radice dell'heap da convertire
 * @size: indirizzo per memorizzare la dimensione dell'array
 * Return: array ordinato
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
    return (heap);
}
